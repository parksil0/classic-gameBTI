import styled from '@emotion/styled';

export const Wrapper = styled.div`
  position: absolute;
  bottom: 34px;
  left: 50%;
  width: 100%;
  margin-bottom: 10px;
  transform: translate(-50%, 0%);
`;
