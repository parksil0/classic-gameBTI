import { css } from '@emotion/react';

export const globalStyles = css`
  html,
  input,
  textarea,
  button {
    font-family: 'Neo둥근모', 'neodgm';
  }

  body {
    margin: 0;
    padding: 0;
  }

  h1,
  h2,
  h3,
  p {
    margin: 0;
  }

  button {
    cursor: pointer;
    border: none;
    color: inherit;
  }
`;
